int n;

int y = 0;  // y -> the final value
byte y_buff[4];

int r;      // r -> the desired value

int e, e_1, e_2, e_3;      // e -> the error signal

int u, u_1, u_2, u_3;      // u -> the signal after PID
byte u_buff[4];
